# 合并到 main 后自动根据 package.json 版本号打 tag
# 使用 PAT（contents:write）推送 tag，确保能触发 docker.yml 的构建

name: Auto tag on main

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  auto-tag:
    name: Auto create version tag
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Read package.json version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check if tag already exists
        id: check
        env:
          PAT_GITHUB_TOKEN: ${{ secrets.PAT_GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          if git ls-remote --tags origin "refs/tags/$TAG" | grep -q "$TAG"; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      # 显式用 PAT 推送 tag，确保触发 docker.yml 构建（GITHUB_TOKEN 会被 GitHub 抑制）
      - name: Create and push tag with PAT
        if: steps.check.outputs.exists == 'false'
        env:
          PAT_GITHUB_TOKEN: ${{ secrets.PAT_GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          git tag "$TAG"
          git push "https://x-access-token:${PAT_GITHUB_TOKEN}@github.com/${{ github.repository }}.git" "$TAG"